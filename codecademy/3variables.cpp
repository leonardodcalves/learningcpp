// "Every variable in C++ must be declared before it can be used!"

#include <iostream>

int main() {

    // Declare a variable
    int year;

    // Initialize a variable
    year = 2025;

    // Declare and initialize a variable
    int ano = 2025;

    // Print
    std::cout << "Today is the year " << year << "\n"; 

    return 0;
}